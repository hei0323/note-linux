#其他运算符

- 比较运算符：
    ```
    # == !=
    [ $a != $b ]
    ```
- 比较运算符(自支持数字)：
```
# -eq -ne -gt -lt -ge -le
[ $a -eq $b ]
    ```
- 布尔运算符：
```
# ! -a -o
[ $a -o $b ]
```
- 逻辑运算符：
```
# && ||
[ $a && $b ]
```
- 字符串运算符
```
# = != -z -n $
[ $a ]
```
- 文件测试运算符
```
#用来判断文件属性如 [ -w file_path ]
```